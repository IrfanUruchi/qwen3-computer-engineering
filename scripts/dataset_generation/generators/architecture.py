from ._scenario import Scenario, register

SCENARIOS = [
    Scenario('false_sharing', 'cache-coherence', 'false sharing', 'independent variables on one cache line cause ownership invalidations between writers', 'HITM/coherence counters and padded-layout A/B tests', 'separate hot writers onto different lines or shard updates', 'logical independence does not imply coherence independence', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('cache_conflict', 'cache', 'set-associativity conflict misses', 'addresses mapping to too few cache sets can thrash despite a small total working set', 'misses while varying alignment/stride with constant bytes', 'change layout/padding/tiling to spread set use', 'nominal cache capacity ignores associativity', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('cache_capacity', 'cache', 'working-set capacity misses', 'a working set larger than effective cache capacity evicts lines before reuse', 'miss rate versus working-set size sweep', 'improve blocking/reuse or reduce working set', 'high miss rate is not automatically a conflict problem', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('cache_compulsory', 'cache', 'cold compulsory misses', 'the first reference to a cache line must fetch it unless prefetched already', 'first-touch versus repeated-pass counters', 'separate cold-start cost from steady-state locality', 'optimizing steady-state reuse cannot remove all first-touch traffic', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('branch_random', 'branch-prediction', 'unpredictable data-dependent branches', 'near-random outcomes defeat predictor history and incur pipeline recovery cost', 'branch-miss counters plus branchless A/B', 'use data/layout/vector techniques when they reduce mispredict cost', 'branchless code can trade branches for extra instructions', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('branch_alias', 'branch-prediction', 'predictor aliasing', 'different branches can compete for predictor state and interfere despite each having local patterns', 'mispredict changes under code layout or branch separation', 'measure before restructuring layout', "one branch's local bias does not guarantee independent predictor state", ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('tlb_reach', 'tlb', 'TLB reach', 'translation cache entries times page size bound the memory that can be covered without page walks', 'TLB miss/page-walk counters versus working-set size', 'use locality or larger pages when tradeoffs justify it', 'cache hits do not eliminate TLB misses', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('page_walk', 'tlb', 'page-walk cost', 'TLB misses trigger multi-level translation walks that consume memory/cache bandwidth', 'page-walk cycles and page-table cache behavior', 'reduce TLB pressure and improve page locality', 'TLB miss rate alone does not show walk cost', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('prefetch_stride', 'prefetch', 'hardware prefetch stride detection', 'prefetchers recognize limited spatial/stride patterns and have finite resources', 'bandwidth/miss counters while varying stride', 'reorder/block access or tune software prefetch only if measured', 'software prefetch can waste bandwidth and pollute caches', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('prefetch_pollution', 'prefetch', 'prefetch cache pollution', 'prefetched lines can evict more useful data when predictions are wrong', 'useful-hit rate and memory traffic with prefetch on/off', 'reduce prefetch distance/aggressiveness or restructure access', 'more prefetched bytes are not always better', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('write_allocate', 'cache', 'write-allocate read-for-ownership traffic', 'store misses may fetch a line before overwriting it, adding traffic for streaming writes', 'memory bytes per output byte and non-temporal-store A/B', 'use streaming stores when reuse/order constraints permit', 'non-temporal stores hurt when data is reused soon', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('store_buffer', 'memory-ordering', 'store-buffer visibility ordering', 'stores can remain buffered and become visible later than programmer intuition without synchronization', 'litmus tests and architecture memory-model reasoning', 'use language-level atomics/fences for required ordering', 'x86 observations are not a portable proof', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('load_store_forward', 'pipelines', 'load-store forwarding stalls', 'a load overlapping an earlier store can stall when size/alignment patterns prevent fast forwarding', 'pipeline counters and alignment/size A/B', 'align/restructure accesses or avoid problematic overlap', 'a cache hit can still stall on dependency machinery', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('unaligned_scalar', 'alignment', 'unaligned scalar accesses', 'some architectures trap or penalize misaligned multi-byte loads/stores', 'fault addresses/instruction type and alignment tests', 'decode bytes safely or align storage', 'x86 tolerance does not define portable behavior', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('unaligned_simd', 'simd', 'unaligned vector accesses', 'SIMD instruction requirements and penalties differ by ISA and microarchitecture', 'instruction traces and aligned/unaligned A/B', 'use correct unaligned instructions or align/peel buffers', 'vector width alone does not guarantee safe arbitrary addresses', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('simd_tail', 'simd', 'vector tail handling', 'the final elements may not fill a whole vector and careless code can read beyond the object', 'boundary-length tests under sanitizers', 'use masked/tail loops or proven padding', 'loads beyond logical length are not safe merely because they stay in a mapped page', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('aos_soa', 'data-layout', 'AoS versus SoA layout', 'field grouping changes cache-line utilization and vectorization for field-wise loops', 'bytes loaded per useful field and vectorization reports', 'match layout to dominant access pattern', 'SoA is not universally superior when most fields of one object are consumed together', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('numa_remote', 'numa', 'remote NUMA access', 'pages placed on one socket cost extra latency/interconnect bandwidth for threads on another', 'local/remote memory counters and bandwidth', 'parallel first-touch or explicit NUMA placement', 'CPU affinity alone does not migrate existing pages', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('numa_bandwidth', 'numa', 'per-socket memory bandwidth saturation', 'adding cores cannot scale once memory controllers/interconnect reach sustained throughput', 'DRAM bandwidth plateau versus throughput', 'reduce bytes/work or spread data/controllers appropriately', 'high CPU utilization does not prove compute-bound execution', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('smt_contention', 'smt', 'SMT sibling contention', 'logical siblings share execution/cache resources and can slow each other on resource-heavy kernels', 'same-core sibling versus separate-core A/B', 'place complementary workloads or reserve physical cores for latency-critical work', 'two logical CPUs are not two full independent cores', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('frontend_icache', 'frontend', 'instruction-cache pressure', 'large hot code footprints can evict instructions and starve the front end', 'I-cache misses and front-end stall counters', 'reduce hot-path footprint or improve layout', 'low data-cache miss rate does not imply the pipeline is fed', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('uop_cache', 'frontend', 'decoded-instruction cache pressure', 'loops that overflow decoded-op caches may fall back to more expensive decode paths', 'front-end counters and code-size A/B', 'simplify/unroll judiciously based on measurement', 'more unrolling can hurt front-end capacity', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('rob_capacity', 'out-of-order', 'reorder-buffer pressure', 'long-latency operations and dependency chains can fill the out-of-order window and stop new progress', 'backend stall and outstanding-miss counters', 'increase locality/parallelism or reduce long dependency chains', 'high instruction-level parallelism needs finite hardware tracking resources', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('memory_level_parallelism', 'memory', 'memory-level parallelism', 'independent cache misses can overlap only up to hardware miss-tracking/queue limits', 'outstanding misses and latency-versus-bandwidth behavior', 'restructure to expose independent requests without exceeding bandwidth', 'one miss latency does not directly predict streaming throughput', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('atomic_hotspot', 'coherence', 'global atomic hotspot', 'all cores repeatedly modify one cache line, serializing ownership', 'atomic throughput and coherence traffic', 'shard counters or batch updates', 'lock-free syntax does not mean contention-free execution', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('fence_cost', 'memory-ordering', 'full memory-fence cost', 'strong fences can drain ordering machinery and limit reordering/overlap', 'cycle counts and fence frequency in profiles', 'use the weakest synchronization that is still correct', 'removing fences for speed without a memory-model proof is unsafe', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('release_acquire', 'memory-ordering', 'release/acquire publication', 'a release store and observing acquire load establish visibility of prior initialization', 'litmus/stress tests plus memory-model proof', 'use release/acquire or stronger synchronization at the publication edge', 'relaxed pointer publication can expose partially initialized state', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('cache_inclusion', 'cache', 'inclusive-cache eviction interactions', 'on inclusive hierarchies, eviction from a shared cache can invalidate corresponding private-cache lines', 'cache hierarchy counters and working-set experiments', 'account for actual microarchitecture when reasoning about eviction', 'all processors use the same inclusion policy', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('victim_cache', 'cache', 'victim-cache effectiveness', 'a small victim structure can capture conflict-evicted lines and reduce repeated misses', 'miss reductions for conflict-heavy patterns', 'tune layout first and use hardware facts in analysis', 'capacity accounting that ignores victim structures can mispredict behavior', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('dram_row_locality', 'dram', 'DRAM row-buffer locality', 'access order affects row hits/misses and therefore latency/bandwidth', 'memory-controller row-hit counters or controlled stride tests', 'improve spatial/order locality when it does not harm higher-level caches', 'all DRAM accesses do not have identical latency', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('bank_conflict', 'dram', 'memory-bank conflicts', 'simultaneous accesses mapping to the same bank serialize more than well-distributed accesses', 'bank-level counters or address-mapping experiments', 'change layout/interleaving/stride to distribute accesses', 'peak bandwidth assumes enough bank-level parallelism', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('pcie_width', 'interconnect', 'negotiated PCIe link width', 'actual generation/width and topology bound transfer rate independently of slot marketing labels', 'negotiated link state and point-to-point benchmarks', 'fix placement/link negotiation or adjust workload partitioning', 'advertised slot capability is not achieved bandwidth', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('pcie_root', 'interconnect', 'PCIe root-complex topology', 'peer traffic may traverse bridges/root complexes or host memory when direct P2P is unavailable', 'topology map, P2P support, directional bandwidth', 'place communicating devices on favorable topology or reduce traffic', 'two devices on PCIe are not automatically direct peers', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('dma_coherency', 'memory', 'DMA cache-coherency boundaries', 'noncoherent devices/CPUs require explicit cache maintenance and ownership transitions', 'stale-buffer tests and platform DMA API traces', 'use the platform DMA mapping/sync protocol', 'volatile does not make caches coherent', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('mmio_order', 'memory-ordering', 'MMIO ordering', 'device-register accesses can have ordering rules different from normal cached memory', 'device trace and architecture barrier requirements', 'use prescribed MMIO accessors/barriers', 'ordinary language volatile alone may not provide required device ordering', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('interrupt_affinity', 'interrupts', 'interrupt affinity and cache locality', 'moving interrupts among CPUs changes queue/driver cache locality and may overload one CPU', 'per-CPU IRQ rate and softirq/load counters', 'align queues/IRQs/workers with NUMA and measured load', 'one IRQ per core is not automatically balanced', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('endianness', 'data-representation', 'endianness conversion', 'multi-byte values serialized in one byte order must be converted explicitly on hosts with another', 'known-vector tests across architectures', 'use fixed wire-format conversion helpers', 'casting bytes to host integers assumes host representation', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('bitfield_layout', 'data-representation', 'C bitfield layout portability', 'bit ordering/padding of language bitfields can be implementation-defined', 'cross-compiler/architecture layout tests', 'serialize masks/shifts explicitly for wire/register formats', 'source-level bitfield order is not a stable protocol encoding', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
]

register('computer-architecture', 'architecture', SCENARIOS, offset=9)
