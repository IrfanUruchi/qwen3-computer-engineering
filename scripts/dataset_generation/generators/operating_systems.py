from ._scenario import Scenario, register

SCENARIOS = [
    Scenario('cow_fault', 'virtual-memory', 'copy-on-write fault behavior', 'a write to a shared COW page allocates/copies a private page without necessarily reading backing storage', 'minor/major fault counters plus PSS/private-dirty changes', 'classify and tune based on actual backing I/O and memory pressure', 'COW allocation is not automatically a major fault', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('demand_zero', 'virtual-memory', 'demand-zero anonymous faults', 'first touch of anonymous memory can allocate a zero-filled page with no disk read', 'fault counters and page allocation traces', 'treat these as normal minor faults unless backing I/O is actually required', 'page allocation work is not the criterion for major faults', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('swap_thrash', 'virtual-memory', 'swap thrashing', 'the working set exceeds effective RAM and pages are repeatedly evicted and faulted back', 'swap-in/out rate, major faults, PSI memory stalls', 'reduce working set or provide memory/headroom and fix limits', 'high CPU utilization can coexist with memory-thrash stalls', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('direct_reclaim', 'virtual-memory', 'synchronous direct reclaim', 'an allocating thread can block reclaiming memory when free-page watermarks cannot satisfy demand', 'request-thread stacks, reclaim counters, memory PSI', 'reduce pressure/provision headroom and avoid problematic high-order allocations', 'adding CPU does not remove synchronous reclaim', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('compaction_stall', 'virtual-memory', 'memory compaction stalls', 'large contiguous allocation requests can trigger compaction even with free memory available', 'compaction counters, allocation-order traces, latency spikes', 'reduce high-order demand or reserve suitable memory earlier', 'free bytes alone do not imply contiguous blocks exist', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('thp_collapse', 'virtual-memory', 'Transparent Huge Page collapse cost', 'background/synchronous hugepage creation can add compaction/copy work for latency-sensitive workloads', 'THP counters, collapse time, page-fault latency', 'compare always/madvise/never-style policy under the workload', 'THP is neither universally good nor universally bad', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('oom_score', 'memory-management', 'OOM victim selection', 'the kernel chooses among eligible processes using policy/score rather than simply the largest RSS', 'OOM logs, oom_score_adj, cgroup membership', 'set policy deliberately and keep critical daemons protected only when justified', 'protecting everything from OOM can leave no viable victim', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('cgroup_memory_high', 'cgroups', 'memory.high throttling', 'crossing memory.high applies pressure/throttling before memory.max kills', 'cgroup memory.events, PSI, reclaim time', 'tune high/max from working-set measurements and SLOs', 'memory.high is not the same failure mode as OOM kill', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('cgroup_pids', 'cgroups', 'pids.max exhaustion', 'fork/thread creation can fail when the cgroup task limit is reached', 'pids.current/events and creation errors', 'bound worker/process creation and size pids.max with operational headroom', 'raising the limit alone does not fix runaway spawning', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('cpu_quota', 'cgroups', 'CFS CPU quota throttling', 'a runnable service can be throttled after consuming its quota even while host CPUs are idle', 'cpu.stat throttled_usec/nr_throttled plus latency', 'adjust quota or workload placement based on required CPU budget', 'host-wide idle CPU does not guarantee a cgroup may run', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('runqueue', 'scheduler', 'run-queue oversubscription', 'too many runnable CPU-bound threads add scheduling/cache cost after cores are saturated', 'run-queue length, context switches, migrations, throughput', 'sweep concurrency and match workers to workload behavior', 'more runnable threads are not more compute capacity', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('migration', 'scheduler', 'CPU migration locality loss', 'moving cache-sensitive work among CPUs can discard hot cache/TLB state', 'migration counts and cache/TLB misses', 'test topology-aware affinity while preserving load balance', 'pinning everything can create hotspots', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('rt_throttle', 'scheduler', 'real-time throttling', 'RT runtime limits can intentionally throttle real-time tasks to protect the system', 'sched/rt runtime stats and trace scheduling gaps', 'budget RT runtime and priorities from bounded critical work', 'raising RT priority/runtime without bounds can starve the machine', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('priority_inversion', 'scheduler', 'priority inversion', 'medium-priority work can preempt the low-priority lock owner needed by a high-priority waiter', 'scheduler/mutex trace showing waiter and owner', 'use priority inheritance/ceiling and bound the critical section', 'priority inheritance does not solve deadlock', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('writer_starvation', 'synchronization', 'reader/writer-lock writer starvation', 'continuous reader admission can indefinitely delay an exclusive waiter under reader-preference policies', 'writer wait distribution and lock admission traces', 'choose a fair/writer-aware lock or different read-mostly design', 'fairness can trade peak reader throughput for bounded writer latency', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('lock_convoy', 'synchronization', 'lock convoying', 'many sleepers repeatedly wake and hand off one contended lock, adding scheduler overhead', 'mutex wait/hold times and context switches', 'reduce acquisition frequency, shard state, or lower excess concurrency', 'short individual critical sections can still convoy under high contention', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('futex_lostwake', 'synchronization', 'futex expected-value protocol', 'the kernel-side expected-value check closes the user-space check-then-sleep race', 'forced state changes around wait and EAGAIN returns', 'recheck the predicate after every wake/immediate return', 'wake notifications are not predicates', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('seqlock_retry', 'synchronization', 'seqlock reader retry', 'readers must reject snapshots that overlap a writer update', 'sequence values before/after reads', 'accept only equal even sequence values and ensure object lifetime is safe', 'seqlocks do not protect pointers whose target can disappear', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('rcu_grace', 'synchronization', 'RCU grace-period reclamation', 'removed objects cannot be freed until all pre-existing readers have exited their read-side critical sections', 'grace-period/read-side traces and UAF tests', 'defer reclamation until the grace period completes', 'logical removal is not equivalent to safe memory reclamation', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('robust_ownerdead', 'synchronization', 'robust process-shared mutex recovery', 'owner death leaves protected invariants suspect even though the next locker gains ownership', 'forced owner crash plus invariant checks', 'repair state and mark consistent only after successful recovery', 'EOWNERDEAD is not an ordinary lock failure', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('pagecache_writeback', 'io', 'dirty page-cache writeback', 'buffered writes can outrun storage temporarily until dirty limits force writers to wait', 'dirty/writeback counters, device latency, application stalls', 'match sustained write rate to device capacity and tune only after measurement', 'larger dirty limits can lengthen eventual stalls', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('readahead', 'io', 'readahead mismatch', 'sequential heuristics can fetch unused pages for sparse random access', 'application bytes versus block-device bytes and cache pollution', 'use access hints/readahead policy appropriate to the workload', 'globally disabling readahead can harm sequential workloads', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('major_fault', 'virtual-memory', 'major page-fault latency', 'faults requiring backing-store reads stall on storage and can dominate tail latency', 'major-fault counts correlated with block I/O', 'reduce cold working-set misses or improve backing-store locality', 'total fault count alone mixes cheap minor faults with expensive major faults', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('numa_autobalance', 'numa', 'automatic NUMA balancing', 'the kernel may migrate pages/tasks based on sampled access, adding overhead while trying to improve locality', 'NUMA hint faults, page migrations, local/remote traffic', 'compare automatic balancing with explicit placement for the workload', 'NUMA balancing overhead is workload-dependent', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('numa_interleave', 'numa', 'interleaved memory placement', 'interleaving can spread bandwidth for evenly shared data but forces some remote access', 'per-node memory bandwidth and remote-access counters', 'compare interleave with first-touch/parallel placement', 'interleaving is not ideal for strongly owned data', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('hugetlb_reserve', 'virtual-memory', 'explicit hugepage reservation', 'hugetlb pages may need reservation/availability before the application starts', 'HugePages counters and allocation failures', 'reserve the required pool early and validate NUMA placement', 'ordinary free memory does not guarantee hugetlb availability', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('mlock_budget', 'virtual-memory', 'locked-memory budgeting', 'mlock prevents reclamation and consumes resident memory under RLIMIT/policy', 'locked memory counters and memlock limits', 'lock only the latency-critical working set and prefault if needed', 'locking the whole process can shift pressure to the rest of the system', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('clock_monotonic', 'timekeeping', 'monotonic timeout deadlines', 'wall-clock steps can make elapsed-time logic expire early or late', 'clock-step tests and monotonic elapsed measurements', 'use a suitable monotonic clock for durations', 'calendar time and elapsed time are different clock domains', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('boottime_clock', 'timekeeping', 'suspend-inclusive timeout semantics', 'some monotonic clocks stop during suspend while boottime-style clocks include it', 'suspend/resume tests of deadline behavior', 'choose the clock whose suspend semantics match the requirement', 'all monotonic clocks do not imply identical suspend behavior', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('zombie_reap', 'processes', 'zombie child accumulation', 'exited children retain process-table entries until their status is collected', 'process states and parent PID relationships', 'reap with wait-family calls and handle multiple concurrent exits', 'zombies are already dead and cannot be killed away', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('orphan_adoption', 'processes', 'orphaned-process reparenting', 'when a parent exits, children are adopted by a subreaper/init-like process depending on the environment', 'PPid/subreaper traces', 'design lifecycle supervision explicitly', 'assuming a child dies automatically with its parent is unsafe', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('pid_reuse', 'processes', 'numeric PID reuse races', 'a stored PID can later refer to a different process after the original exits', 'process start times/pidfds around delayed actions', 'use pidfds or other process-instance handles where available', 'numeric PID alone is not a stable identity token', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('signal_delivery', 'signals', 'process-directed signal delivery in multithreaded programs', 'eligible threads and signal masks determine which thread handles an asynchronous signal', 'per-thread masks and handler traces', 'centralize signal handling or set masks deliberately', "signals are not automatically delivered to the thread that 'caused' interest", ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('timer_overrun', 'timers', 'timer expiration overruns', 'periodic timers can accumulate multiple expirations while the consumer is delayed', 'overrun/expiration counters', 'account for missed periods or intentionally coalesce work', 'one wakeup does not prove exactly one period elapsed', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('epoll_thundering', 'eventing', 'thundering herd on shared readiness', 'multiple waiters may wake for shared events depending on the primitive/configuration', 'wake counts per event and useless wakeups', 'use one-shot/exclusive/ownership techniques appropriate to the event source', 'more waiters can increase wakeup overhead without adding work', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('io_uring_lifetime', 'io', 'asynchronous buffer lifetime', 'buffers referenced by queued I/O must remain valid until completion', 'submission/completion traces plus poisoned-buffer tests', 'tie memory ownership to completion or use registered/pinned resources correctly', 'returning from the submitter does not mean the kernel is done with the buffer', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('oom_cgroup', 'cgroups', 'cgroup-local OOM behavior', 'a memory cgroup can OOM within its limit while the host still has free memory', 'memory.events and host-vs-cgroup free memory', 'size the cgroup from real working set and inspect local OOM policy', 'host free memory does not bypass a cgroup memory.max', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('swapiness', 'virtual-memory', 'swap/reclaim policy tradeoffs', 'reclaim policy balances anonymous memory and page cache under pressure rather than obeying one simple threshold', 'vmstat, refaults, swap I/O, page-cache hit behavior', 'tune only after identifying which working set is harmed', 'swappiness is not a direct percentage of RAM to swap', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('kswapd_balance', 'virtual-memory', 'background reclaim watermarks', 'kswapd reclaims in the background to restore watermarks before allocators enter direct reclaim', 'kswapd CPU, pgscan/pgsteal, free watermarks', 'maintain enough headroom so background reclaim can keep up', 'seeing kswapd active is not itself a failure', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
]

register('operating-systems', 'os', SCENARIOS, offset=6)
