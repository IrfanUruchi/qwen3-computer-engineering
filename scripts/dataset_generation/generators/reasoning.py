from ._scenario import Scenario, register

SCENARIOS = [
    Scenario('amdahl', 'performance-modeling', "Amdahl's Law speedup bound", 'the serial fraction bounds unlimited-processor speedup', 'serial fraction and measured scaling curve', 'optimize the limiting serial portion when added parallelism gives diminishing returns', 'more processors cannot remove inherently serial work', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('gustafson', 'performance-modeling', 'Gustafson scaled-speedup reasoning', 'larger problem sizes can keep parallel resources busy while serial time stays comparatively small', 'fixed-time scaled workload measurements', 'use the model only when workload size legitimately scales with resources', 'Gustafson does not contradict Amdahl; it answers a different scaling question', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('queue_stability', 'queueing', 'queue stability', 'long-run arrival rate at or above sustainable service rate makes backlog unbounded', 'arrival/completion rates and queue-depth trend', 'reduce admitted load or increase sustainable capacity', 'a bigger queue delays failure but does not stabilize an overloaded system', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('little_law', 'queueing', "Little's Law assumptions", 'L=λW relates average number, throughput, and time in a stable system', 'same-window measurements of L, lambda and W', 'apply only in a stable/conserved flow regime', 'a continuously growing queue is not steady state', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('util_knee', 'capacity', 'utilization latency knee', 'queueing delay can rise sharply before nominal throughput ceiling is reached', 'p95/p99 versus offered-load curve', 'operate below the SLO knee with headroom', 'benchmark maximum throughput is not safe production capacity', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('failover_headroom', 'capacity', 'N+1/failure headroom', 'capacity planning must consider what survives the failure, not only normal total utilization', 'failover load tests and surviving-node utilization', 'provision surviving capacity for target failover load plus margin', '60% total fleet use can overload half the fleet after a zone loss', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('tail_slo', 'slo', 'tail-latency objectives', 'mean latency hides a small but user-visible slow tail', 'histograms and p95/p99 by endpoint', 'set percentile/error-budget objectives tied to product impact', 'one universal percentile is not correct for every service', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('error_budget', 'slo', 'error-budget policy', 'availability objectives imply a finite amount of tolerated failure over a window', 'successful/failed request accounting over the SLO window', 'use budget consumption to guide rollout/risk decisions', 'an SLO is not the same as promising zero incidents', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('rto_rpo', 'reliability', 'RTO versus RPO', 'recovery time and acceptable data loss constrain different parts of backup/replication design', 'disaster-recovery drills measuring lost data and restore duration', 'design backup frequency and restoration automation to meet both', 'frequent backups can meet RPO while still missing RTO', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('availability_series', 'reliability', 'series dependency availability', 'a request depending on several independent components succeeds only when all required components are available', 'dependency availability and joint-failure assumptions', 'reduce required synchronous dependencies or add tolerated redundancy', 'multiplying independent probabilities is invalid when failures are correlated', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('redundancy_corr', 'reliability', 'correlated failure in redundancy', 'replicas sharing power/network/zone/software can fail together, reducing real redundancy', 'failure-domain mapping and joint incident history', 'spread replicas across independent failure domains', 'replica count alone does not measure independence', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('energy_job', 'efficiency', 'energy per completed job', 'energy equals power integrated over execution time, not instantaneous watts alone', 'power telemetry and job duration', 'compare watt-hours/joules for identical completed work', 'lower power can use more total energy if runtime grows enough', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('cost_perf', 'efficiency', 'cost per useful throughput', 'cheaper hardware may deliver worse total cost if more units/power/operator time are required', 'end-to-end cost and sustained useful work', 'compare normalized cost per target SLO/throughput', 'purchase price alone is not total cost', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('batch_latency', 'queueing', 'batch-size throughput/latency tradeoff', 'larger batches amortize fixed overhead but wait longer to fill and can increase tail latency', 'throughput and per-request latency versus batch size', 'choose batch policy from SLO and workload arrival pattern', 'max-throughput batch size is not necessarily acceptable interactively', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('load_shed', 'resilience', 'load shedding before collapse', 'admission control preserves bounded latency for accepted work when demand exceeds capacity', 'queue depth, rejection rate, accepted-request latency', 'reject/degrade optional work before internal queues saturate', 'serving every request poorly is not always more available', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('canary', 'operations', 'canary rollout risk control', 'small initial exposure limits blast radius and provides evidence before broad rollout', 'canary/control metrics and rollback timing', 'promote only when predefined health criteria pass', 'a canary without comparison/stop criteria is just an early deployment', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('capacity_units', 'capacity', 'throughput units and bottleneck conservation', 'requests/s alone can mislead when request cost varies widely', 'CPU-ms, bytes, queries, tokens, or other work units per request', 'normalize capacity to the bottleneck resource and workload mix', 'two requests are not necessarily equal units of work', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('percent_change', 'quantitative', 'percentage change interpretation', 'relative percentage and absolute percentage-point change are different quantities', 'baseline and final values with explicit units', 'state whether the result is relative percent or percentage points', "reporting '20% increase' without a baseline can be ambiguous", ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('confidence_interval', 'measurement', 'benchmark uncertainty', 'small timing differences can be noise when run-to-run variance is comparable to the claimed gain', 'repeated runs and confidence intervals/distribution', 'report variance and use enough repetitions before declaring improvement', 'one fastest run is not evidence of a stable optimization', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('measurement_bias', 'measurement', 'observer/benchmark bias', 'instrumentation, warm caches, turbo, background load or selection of best runs can bias conclusions', 'controlled repetitions with randomized order and environment telemetry', 'design A/B experiments that hold confounders constant', 'a precise number can still be systematically biased', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
]

register('engineering-reasoning', 'reasoning', SCENARIOS, offset=8)
