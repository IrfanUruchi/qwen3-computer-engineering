from ._scenario import Scenario, register

SCENARIOS = [
    Scenario('short_read', 'posix-io', 'partial `read()` completion', 'POSIX reads may return fewer bytes than requested without error', 'returned counts and syscall traces', 'loop while preserving offset until complete/EOF/error', 'one positive return is not the same as full completion', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('short_write', 'posix-io', 'partial `write()` completion', 'writes can make partial progress and signals/nonblocking state can interrupt progress', 'returned counts, EINTR/EAGAIN, and sink bytes', 'loop from the unsent offset and integrate readiness for nonblocking descriptors', 'retrying from offset zero can duplicate bytes', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('accept_cloexec', 'file-descriptors', 'atomic close-on-exec accept', 'setting CLOEXEC after accept creates a race with another thread calling exec', 'descriptor flags plus concurrent fork/exec stress', 'use accept4/SOCK_CLOEXEC or another atomic creation flag', 'post-creation flag changes are not race-free in multithreaded code', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('dup2_atomic', 'file-descriptors', 'descriptor replacement with `dup2`/`dup3`', 'close-then-dup can race with another descriptor allocation into the target number', 'concurrent descriptor-allocation stress and fd tables', 'use the atomic replacement primitive and handle same-fd semantics', 'manual close+dup is not equivalent', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('pipe_deadlock', 'ipc', 'bidirectional pipe backpressure', 'finite pipe buffers can make each side block while waiting for the other direction to drain', 'blocked syscall stacks and pipe occupancy', 'read and write concurrently or use nonblocking/event-driven flow control', 'large pipes only postpone a sustained mutual-wait pattern', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('pipe_atomicity', 'ipc', 'multi-writer pipe record atomicity', 'writes above the platform atomicity bound can interleave between writers', 'record corruption traces and write sizes relative to PIPE_BUF', 'frame records within the guaranteed bound or serialize writers', 'a pipe preserves bytes, not arbitrary application messages', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('sigpipe', 'signals', 'SIGPIPE on closed peer', 'writing to a closed pipe/socket may terminate the process under the default signal disposition', 'termination signal and peer-close timing', 'suppress/handle SIGPIPE appropriately and process EPIPE as I/O failure', 'missing logs can be caused by signal termination before error handling', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('sigchld_race', 'signals', 'SIGCHLD child-reaping races', 'multiple children can exit before one notification is processed and signals may coalesce', 'waitpid results and unreaped process states', 'reap in a loop until no more exited children remain', 'one signal does not imply exactly one child exit', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('fork_mutex', 'processes', 'fork from a multithreaded process', 'the child inherits lock state but only the calling thread survives', 'deadlocked mutex ownership after fork', 'keep the child path async-signal-safe/minimal or use posix_spawn', 'library locks can remain owned by vanished threads', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('posix_spawn', 'processes', 'spawn versus fork+exec in multithreaded programs', 'spawn avoids running arbitrary post-fork library code in a copied multithreaded address space', 'child startup traces and failure reproduction under thread load', 'prefer posix_spawn where its file-action model is sufficient', 'fork+complex-child-setup is not free just because exec follows soon', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('mmap_truncate', 'virtual-memory', 'mapped-file truncation hazards', 'access beyond the new file extent can fault after another process truncates the file', 'file-size timeline and fault address', 'coordinate file lifetime/size or remap safely', 'a valid mapping at creation does not freeze backing-file size', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('mmap_shared', 'virtual-memory', 'MAP_SHARED visibility and persistence', 'visibility to peer mappings and durability to storage are separate properties', 'peer reads plus msync/fsync/crash tests', 'use the required synchronization and durability primitives for the actual guarantee', 'seeing a write in another process does not prove crash durability', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('msync_durability', 'virtual-memory', 'mapped write durability', 'dirty mapped pages may be visible in memory before they are durable on storage', 'crash/power-loss tests and filesystem semantics', 'use platform/filesystem durability primitives and persist required metadata ordering', 'msync semantics are not identical across every filesystem/platform', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('append_atomicity', 'filesystems', 'concurrent O_APPEND writers', 'append positions are chosen atomically by the kernel but application records may still span multiple writes', 'file record boundaries under concurrent writers', 'emit each required atomic record in one supported operation or serialize higher-level records', 'O_APPEND does not make a sequence of writes one transaction', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('rename_durability', 'filesystems', 'rename crash durability', 'rename can be namespace-atomic while the new file or directory entry is not yet durable after power loss', 'crash testing plus fsync ordering', 'fsync the file before rename and the containing directory afterward where required', 'atomic visibility and durable persistence are different guarantees', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('sparse_file', 'filesystems', 'sparse-file hole handling', 'logical file length can greatly exceed allocated blocks and hole reads synthesize zeros', 'stat block counts plus SEEK_HOLE/SEEK_DATA where supported', 'preserve holes during copy/backup when semantics require it', 'copying byte-for-byte can accidentally allocate every hole', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('direct_io', 'storage', 'direct-I/O alignment constraints', 'buffer address, offset, and length may need device/filesystem-specific alignment', 'actual addresses plus reported alignment requirements', 'use suitably aligned buffers and compatible offsets/lengths', 'hard-coding 4096 bytes is not portable', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('sendfile_partial', 'zero-copy', 'partial `sendfile()` progress', 'zero-copy syscalls can return short positive progress just like other I/O', 'return values and offset progression', 'loop until the requested range is sent and handle EINTR/EAGAIN appropriately', 'zero-copy does not imply all-or-nothing completion', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('splice_backpressure', 'zero-copy', 'splice through finite pipe buffers', 'kernel zero-copy still uses bounded queues and can block behind a slow sink', 'pipe readiness and downstream throughput', 'use bounded event-driven flow control', 'zero-copy does not remove backpressure', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('recvmsg_ctrunc', 'unix-sockets', 'ancillary-data truncation', 'the control buffer can be too small even when payload bytes arrive', 'MSG_CTRUNC and control-message lengths', 'reject truncated control data and size/validate CMSG records correctly', 'partially parsed descriptor sets are unsafe', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('scm_rights_cleanup', 'unix-sockets', 'received descriptor cleanup on validation failure', 'file descriptors are real resources even if later message validation fails', 'open-fd counts across rejected messages', 'close every received fd on all failure paths', 'discarding a message object does not automatically close received descriptors', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('unix_creds', 'unix-sockets', 'peer credential verification', 'filesystem socket path permissions do not necessarily prove the identity of the connected peer', 'SO_PEERCRED or platform credential messages', 'authenticate the peer using kernel-provided credentials when policy requires it', 'trusting a pathname alone can confuse endpoint identity', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('robust_mutex', 'synchronization', 'robust mutex owner death', 'EOWNERDEAD grants the lock but protected state may be inconsistent', 'forced owner crash and shared-state invariants', 'repair/validate then mark consistent before unlock', 'treating EOWNERDEAD as ordinary success accepts possibly corrupt state', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('process_shared_cond', 'synchronization', 'process-shared condition-variable predicates', 'condition waits can wake spuriously and predicates live in shared memory', 'predicate traces across multiple processes', 'loop on the predicate under the associated process-shared mutex', 'one wakeup is not a proof that the condition is true', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('file_offset_race', 'posix-io', 'shared file-offset races', 'threads sharing one open file description can interfere through the common current offset', 'offset traces and duplicated/skipped regions', 'use pread/pwrite or explicit synchronization when independent offsets are required', 'separate integer fd values can still reference the same open file description', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('pread_threadsafe', 'posix-io', 'position-independent concurrent reads', 'pread reads from an explicit offset without changing the shared file position', 'concurrent range checks', 'use pread for independent random-access reads', 'lseek+read pairs are not atomic with respect to other users of the same offset', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('size_t_overflow', 'integer-safety', 'size_t addition overflow', 'unsigned addition wraps before later bounds checks can inspect the intended sum', 'boundary tests near SIZE_MAX', 'check `b > SIZE_MAX-a` before addition', 'checking only the wrapped sum can accept an undersized allocation', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('ssize_conversion', 'integer-safety', 'signed syscall result conversion', '-1 converted to size_t becomes a huge positive value', 'error-path unit tests', 'keep ssize_t until negative/zero cases are handled', 'unsigned comparison after conversion can treat failure as success', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('aligned_alloc', 'memory', 'aligned allocation size rules', 'some aligned-allocation APIs require size/alignment relationships and correct matching free semantics', 'boundary allocation tests and returned pointer alignment', 'use the platform API contract exactly and check failures', 'alignment requests are not interchangeable across APIs', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('realloc_alias', 'memory', 'realloc pointer invalidation', 'successful realloc may move the object and invalidate aliases to the old address', 'address changes under forced growth', 'update all ownership references from the returned pointer and avoid stale aliases', 'the old pointer cannot be assumed valid after successful realloc', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('strtok_threads', 'libc', 'thread-unsafe tokenizer state', 'legacy tokenizer APIs can store hidden global/static state', 'concurrent parse corruption tests', 'use reentrant/state-explicit parsing', 'functions with hidden global state do not become safe because input buffers differ', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('locale_global', 'libc', 'process-global locale races', 'changing global locale can alter parsing/formatting in unrelated threads', 'concurrent locale-sensitive tests', 'use per-thread/explicit locale APIs where available', 'global locale mutation is not an isolated request setting', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('errno_lifetime', 'libc', 'errno preservation around cleanup', 'later library calls can overwrite errno before it is reported', 'forced failure followed by cleanup calls', 'save errno immediately when needed for later diagnostics', 'errno is thread-local but not immutable', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('tmpfile_race', 'filesystems', 'secure temporary-file creation', 'predictable path creation can race with attackers creating symlinks/files', 'concurrent hostile namespace tests', 'use mkstemp/O_TMPFILE or equivalent atomic secure creation', 'checking nonexistence before create introduces a TOCTOU race', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('path_resolution', 'filesystems', 'descriptor-relative path resolution', 'checking a path and opening it later allows namespace changes between operations', 'symlink-swap race tests', 'resolve beneath trusted dirfds with constrained APIs and operate on returned descriptors', 'canonicalizing a string does not freeze the filesystem namespace', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('eventfd_counter', 'ipc', 'eventfd counter semantics', 'eventfd coalesces increments into a counter rather than preserving one notification object per write', 'read values versus writer event counts', 'design consumers around counter semantics or use a queue for per-event payloads', 'one read is not one producer event', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('timerfd_expiration', 'time', 'timerfd expiration counts', 'if the consumer falls behind, one read can report multiple expirations', 'expiration counts under induced delay', 'process the count or intentionally coalesce work', 'treating every readiness notification as exactly one tick loses missed intervals', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('clock_nanosleep', 'time', 'absolute monotonic sleeps', 'relative sleeps accumulate drift across repeated periodic work', 'long-run period error', 'use absolute deadlines on an appropriate monotonic clock', 'restarting relative sleeps after interruptions compounds drift', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('pthread_once', 'initialization', 'one-time initialization publication', 'ad-hoc boolean checks can race and expose partially initialized state', 'concurrent startup stress', 'use pthread_once or language-supported once semantics', 'a plain initialized flag is not sufficient synchronization', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
]

register('systems-programming', 'systems', SCENARIOS, offset=3)
