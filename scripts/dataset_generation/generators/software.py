from ._scenario import Scenario, register

SCENARIOS = [
    Scenario('schema_compat', 'api-contracts', 'backward-compatible schema evolution', 'old and new binaries can coexist only when readers tolerate fields/versions produced by the other side', 'mixed-version canary tests and serialized payload captures', 'stage additive/read-compatible changes before destructive removals', 'deploy order alone does not repair an incompatible wire/storage contract', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('async_scope', 'concurrency', 'request-scoped asynchronous work', 'child work can outlive its owner unless lifetime and cancellation are structured', 'task counts after request completion plus cancellation traces', 'bind child tasks to a scope and join or cancel them on every exit path', 'fire-and-forget work is not harmless when it retains resources', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('db_retry_scope', 'databases', 'whole-transaction retry boundaries', 'a serialization/deadlock abort invalidates assumptions from earlier statements in the transaction', 'database abort codes and a replay test from fresh reads', 'rollback and rerun the complete logical transaction with bounded retry', 'retrying only the final statement can commit from stale observations', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('migration_dualread', 'databases', 'online data migration with mixed formats', 'during migration, stored rows may exist in both old and new representations', 'shadow reads and version counters across migrated/unmigrated records', 'make readers tolerate both formats and backfill before switching writers fully', 'a one-shot rewrite assumes no concurrent old-version writer remains', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('queue_admission', 'backpressure', 'bounded admission for internal work queues', 'unbounded queues convert overload into memory growth and latency rather than rejecting excess work', 'queue depth, arrival/service rates, and time-in-queue histograms', 'bound the queue and shed or reject before the latency budget is exhausted', 'a larger queue is not additional service capacity', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('retry_amplification', 'resilience', 'multi-layer retry amplification', 'independent retries at several layers multiply one failure into many downstream attempts', 'attempt counts by layer and request correlation IDs', 'choose one retry owner and propagate deadlines/budgets through lower layers', 'small retry counts at each layer do not remain small when multiplied', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('cache_key_scope', 'caching', 'cache-key tenant and variant isolation', 'omitting a dimension from the key can return data produced for a different tenant/version/locale', 'sampled key material and mismatched response provenance', 'include every correctness-relevant dimension or partition caches explicitly', 'high hit rate does not prove cache-key correctness', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('idempotency_state', 'api-semantics', 'durable idempotency state', 'a retry after a crash is safe only if the logical operation result is recorded across the failure boundary', 'fault injection after the external side effect but before the response', 'persist idempotency state atomically with the operation or use an equivalent durable protocol', 'an in-memory response cache disappears exactly when crash recovery needs it', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('circuit_probe', 'resilience', 'half-open circuit recovery', 'reopening full traffic immediately can overload a dependency that has only partially recovered', 'probe success rate, dependency saturation, and circuit-state transitions', 'allow a bounded probe set with jitter and gradual reopening', 'time-based reopening alone can synchronize all callers', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('listener_lifetime', 'resource-management', 'event-listener lifetime ownership', 'publishers can retain request/session objects through callbacks after the logical owner is gone', 'heap retain paths and listener counts over time', 'use scoped subscriptions or guaranteed unregister cleanup', 'raising the heap limit delays retention rather than fixing it', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('pool_starvation', 'databases', 'connection-pool checkout starvation', 'request threads can block before SQL begins when all pool entries are held', 'pool in-use/idle/waiter metrics and checkout duration', 'fix leaked or overlong ownership and bound waiting by request deadlines', 'database CPU can be low while the application is pool-bound', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('feature_rollout', 'operations', 'feature-flag rollout safety', 'a mutable flag changes production behavior without a binary deployment and therefore needs its own safety controls', 'active flag version, cohort metrics, and rollback timing', 'use scoped rollout, safe defaults, audit history, and tested kill-switch behavior', 'a flag is not safe merely because it can be changed quickly', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('serialization_canonical', 'data-formats', 'canonical serialization', 'semantically equal objects can encode differently unless ordering/normalization rules are defined', 'round-trip and canonical-byte property tests', 'define canonical ordering/normalization and test malformed input separately', 'round-trip equality alone does not prove canonical encoding', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('worker_isolation', 'resilience', 'per-dependency bulkhead isolation', 'one slow dependency can consume shared workers/connections and starve unrelated work', 'per-dependency queue depth, concurrency, and timeout traces', 'give dependencies separate bounded budgets and preserve capacity for unrelated paths', 'a larger global pool does not create isolation', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
]

register('software-engineering', 'software', SCENARIOS, offset=0)
