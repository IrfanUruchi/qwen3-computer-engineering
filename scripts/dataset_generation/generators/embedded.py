from ._scenario import Scenario, register

SCENARIOS = [
    Scenario('dma_ring', 'dma', 'DMA circular-buffer overrun', 'the producer can lap unread data when firmware service latency exceeds ring capacity', 'producer/consumer positions plus explicit overrun counters', 'size for worst-case latency and detect lapping', 'reading indices more often does not prove capacity', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('dma_cache', 'dma', 'DMA cache coherency', 'noncoherent CPU/device caches require explicit ownership/cache maintenance around shared buffers', 'stale-data tests and platform DMA API traces', 'use mapping/sync primitives at ownership boundaries', 'volatile does not flush or invalidate caches', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('isr_latency', 'interrupts', 'interrupt latency budgeting', 'long masked sections or higher-priority ISRs delay deadline-critical handlers', 'interrupt latency histogram and mask-duration traces', 'shorten critical sections and prioritize only bounded work', 'average interrupt latency can hide missed worst-case deadlines', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('isr_work', 'interrupts', 'excessive work inside ISR', 'blocking or heavy work in interrupt context extends latency for the rest of the system', 'ISR execution time and nested/missed event counts', 'acknowledge/capture minimal state then defer work', 'fast hardware does not make blocking ISR code safe', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('nested_stack', 'interrupts', 'interrupt nesting stack usage', 'nested handlers consume stack on top of foreground call depth', 'stack high-water marks under forced nesting', 'budget stack from worst-case nesting and keep ISRs small', 'steady-state stack measurements can miss worst-case interrupt depth', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('watchdog_progress', 'watchdog', 'progress-aware watchdog feeding', 'a timer that always feeds the watchdog can remain alive while useful work is deadlocked', 'induced deadlock with feed path still running', 'feed only after health/progress milestones', 'periodic execution alone is not proof of system health', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('brownout', 'power', 'brownout resets', 'load transients can drop supply below the MCU brownout threshold and look like software resets', 'reset-cause flags and oscilloscope rail capture', 'fix power path/decoupling/sequencing while keeping brownout protection', 'reboot symptoms do not imply watchdog failure', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('sleep_wakeup', 'power', 'low-power wake-source configuration', 'deep sleep may disable clocks/peripherals needed by the selected wake source', 'power-state registers and wake-source tests', 'configure only supported wake sources and restore clocks in the right order', 'an interrupt that works while awake may not be able to wake deep sleep', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('adc_alias', 'sampling', 'ADC aliasing', 'content above Nyquist folds into lower apparent frequencies after sampling', 'spectrum and sample-rate sweep', 'increase sample rate and use analog anti-alias filtering', 'digital processing cannot undo ambiguous sampling', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('adc_settle', 'sampling', 'ADC source-settling error', 'high source impedance may not charge the sample-and-hold capacitor within acquisition time', 'measured input versus acquisition-time/source-impedance sweep', 'increase acquisition time or buffer the source', 'ADC resolution specs assume electrical settling requirements are met', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('i2c_stretch', 'i2c', 'I2C clock stretching', 'a slave can hold SCL low while not ready, requiring master support and suitable timeout', 'logic-analyzer SCL timing versus device datasheet', 'enable/support stretching within bounded timeout', 'a low SCL line is not always a stuck bus', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('i2c_stuck', 'i2c', 'I2C stuck-bus recovery', 'an interrupted transaction can leave a slave holding SDA low until clocked through remaining state', 'SCL/SDA waveform at failure and reset state', 'apply platform-approved recovery clocks/reset then reinitialize controller', 'blindly reinitializing the master may not release the slave', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('spi_cs', 'spi', 'SPI chip-select timing', 'devices can require setup/hold/inter-word timing around CS that generic transfers may violate', 'logic-analyzer timing versus datasheet minima', 'configure controller delays or explicit CS sequencing', 'matching clock frequency alone does not prove protocol timing', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('spi_mode', 'spi', 'SPI clock polarity/phase mismatch', 'wrong CPOL/CPHA samples data on the wrong edge and can create bit shifts or unstable values', 'waveforms and known-register reads across modes', 'use the device-specified mode and verify edge timing', 'some low-speed tests can appear to work accidentally', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('uart_baud', 'uart', 'UART baud-rate error', 'oscillator/divider error from transmitter and receiver accumulates across a frame and can exceed sampling margin', 'measured bit periods across temperature/voltage', 'choose accurate clocks/dividers or calibration', 'software parsing cannot fix framing caused by clock mismatch', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('uart_overrun', 'uart', 'UART receive overrun', 'hardware FIFO/data register can be overwritten if firmware does not service it before the next characters arrive', 'overrun status flags and ISR/service latency', 'use DMA/FIFO/priority/buffer sizing appropriate to peak rate', 'a large software ring cannot recover bytes already lost in hardware', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('can_busoff', 'can', 'CAN bus-off recovery', 'repeated transmit errors can force a controller into bus-off to protect the network', 'TEC/REC counters and bus-off status', 'find physical/protocol cause and follow controlled recovery policy', 'immediate endless auto-restart can flood a broken bus', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('can_termination', 'can', 'CAN termination and reflections', 'incorrect termination/branching distorts edges and raises error frames especially at higher bit rates', 'oscilloscope differential waveform and error counters', 'restore correct bus topology/termination', 'software retries do not repair signal integrity', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('flash_wear', 'flash', 'flash erase/program endurance', 'repeated updates to one erase block consume finite cycle endurance', 'write/erase count estimates and wear distribution', 'rotate/log records and reduce/batch writes', 'capacity in bytes is not endurance in update cycles', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('flash_powerfail', 'flash', 'power-loss-safe metadata update', 'interrupted erase/program can leave metadata partially written', 'power-cut tests at every update phase', 'use versioned redundant records and validate newest complete copy', 'a CRC on one mutable copy does not guarantee recoverability', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('boot_ab', 'bootloader', 'A/B firmware rollback', 'a candidate image must be proven healthy before replacing the known-good boot path', 'power-cut and failed-health-milestone tests', 'write inactive slot, authenticate it, trial boot, then commit', 'erasing the old slot before proving the new one removes recovery', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('secure_boot', 'bootloader', 'firmware authenticity chain', 'boot security depends on verifying each executable stage against an anchored trust policy', 'signature-failure tests and key/version metadata', 'verify signatures and anti-rollback policy before execution', 'hashing without an authenticated trust anchor does not prove origin', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('mmio_volatile', 'mmio', 'volatile MMIO access', 'volatile prevents some compiler elision/reordering but does not by itself provide all CPU/device ordering guarantees', 'generated instructions and platform barrier requirements', 'use platform MMIO accessors/barriers', 'volatile is not a cache-coherency or hardware-memory-barrier primitive', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('atomic_width', 'concurrency', 'multi-byte shared-variable tearing', 'an 8-bit MCU may require multiple instructions to read/write a wider counter shared with an ISR', 'stress tests around interrupt boundaries', 'use atomic-width types or short critical sections/atomics', 'volatile does not make a multi-instruction access atomic', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('timer_wrap', 'timers', 'unsigned timer wraparound', 'modular subtraction preserves elapsed intervals across one counter rollover', 'boundary tests around maximum counter value', 'compare `now-start` in unsigned arithmetic under documented interval bounds', 'raw absolute-time ordering fails at wrap', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('debounce', 'gpio', 'mechanical switch bounce', 'one physical transition produces several rapid edges', 'logic-analyzer edge timing and event counts', 'use a nonblocking stable-state timer/state machine', 'busy-wait debounce blocks unrelated real-time work', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('pwm_jitter', 'timers', 'PWM update jitter', 'software-timed edge updates vary with interrupt/scheduler latency', 'edge-period jitter on scope/logic analyzer', 'use hardware timers/DMA for deterministic waveforms', 'average frequency does not describe edge jitter', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('rtc_drift', 'timekeeping', 'RTC oscillator drift', 'ppm frequency error accumulates into wall-time error over long intervals', 'reference-clock comparison over temperature/time', 'calibrate or use a more stable source/synchronization policy', 'one-time clock setting does not correct frequency error', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('crc_scope', 'integrity', 'CRC coverage boundaries', 'a checksum only protects bytes that are actually included and does not authenticate hostile modifications', 'bit-flip tests inside/outside the covered region', 'define canonical covered fields and use MAC/signature when authenticity is required', 'CRC detects accidental corruption, not malicious tampering', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('endianness_wire', 'protocols', 'wire-format endianness', 'host integer byte order may differ from the protocol representation', 'known test vectors on different architectures', 'encode/decode explicit byte order', 'casting packet bytes directly to host integers is not portable', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
]

register('embedded-systems', 'embedded', SCENARIOS, offset=15)
