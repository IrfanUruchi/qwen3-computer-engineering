from ._scenario import Scenario, register

SCENARIOS = [
    Scenario('path_traversal', 'filesystem-security', 'path traversal', 'user-controlled path components can escape an intended root through `..`, absolute paths or symlink resolution', 'hostile path/symlink race tests', 'resolve beneath trusted dirfds with constrained APIs', 'string prefix checks do not make filesystem resolution safe', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('toctou', 'filesystem-security', 'filesystem TOCTOU', 'checking a pathname then reopening it allows namespace changes between check and use', 'symlink-swap race tests', 'open under constrained resolution and validate the returned descriptor', 'canonicalization before open does not make the later open atomic', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('tempfile', 'filesystem-security', 'temporary-file races', 'predictable temporary names can be pre-created or replaced by attackers', 'concurrent hostile namespace tests', 'use atomic secure creation with restrictive permissions', 'checking that a pathname is absent before create is racy', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('constant_time', 'cryptography', 'constant-time secret comparison', 'early-exit equality leaks mismatch position through timing under suitable observation', 'statistical timing tests plus code review', 'use vetted constant-time comparison primitives', 'constant-time compare does not fix an insecure MAC', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('nonce_reuse', 'cryptography', 'AEAD nonce reuse', 'many AEAD modes require nonce uniqueness per key and reuse can break confidentiality/authentication', 'forced duplicate-nonce test vectors', 'guarantee nonce uniqueness or use a misuse-resistant construction designed for the threat', 'authentication tags do not make nonce reuse safe', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('key_rotation', 'cryptography', 'cryptographic key rotation', 'old data/sessions may still require prior keys while new operations should use the new key', 'key-version coverage and decrypt/verify tests', 'version keys explicitly and stage rotation/revocation', 'overwriting one key file can make historical data unrecoverable', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('jwt_aud', 'authentication', 'JWT audience validation', 'a valid signature does not prove a token was issued for this service', 'issuer/audience/expiry claim tests', 'validate issuer, audience, time and algorithm policy explicitly', 'signature validity alone is not authorization', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('jwt_alg', 'authentication', 'JWT algorithm confusion', 'accepting algorithms based on untrusted token metadata can route verification through unintended logic', 'malformed/algorithm-substitution tests', 'pin allowed algorithms and key types in verifier configuration', 'the `alg` field is input, not trusted policy', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('mtls_san', 'authentication', 'mTLS service identity', 'trusted chains still require SAN identity matching and appropriate EKU/policy', 'certificate chain plus SAN/EKU inspection', 'issue/verify identities according to service naming policy', 'CA trust is not equivalent to endpoint identity', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('csrf', 'web-security', 'CSRF protection for cookie-authenticated state changes', 'ambient cookies can be sent by cross-site requests unless state-changing actions require same-origin proof', 'cross-site form/fetch tests', 'use SameSite plus anti-CSRF/origin controls as appropriate', 'SameSite alone may not cover every required cross-site flow', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('ssrf', 'web-security', 'SSRF to internal services', 'server-side URL fetching can give attackers network reachability they do not have directly', 'blocked-address test corpus and redirect/DNS rebinding cases', 'allowlist destinations/protocols and revalidate after redirects/resolution', 'checking only the original hostname can miss later resolution changes', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('sql_injection', 'injection', 'SQL injection', 'concatenating untrusted input into SQL can change query structure', 'malicious quote/operator test inputs', 'use parameterized queries and constrain dynamic identifiers separately', 'escaping is not interchangeable across SQL contexts', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('command_injection', 'injection', 'shell command injection', 'passing untrusted text through a shell gives metacharacters control over command structure', 'metacharacter/argument-boundary tests', 'avoid shell invocation and pass argv directly', 'quoting one variable manually is easy to get wrong', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('deserialization', 'memory-safety', 'unsafe object deserialization', 'deserializers that instantiate arbitrary types can execute gadget behavior or violate invariants', 'untrusted-type payload tests', 'use data-only schemas and explicit type allowlists', 'cryptographic transport does not make attacker-controlled payloads trusted', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('capability_broker', 'sandboxing', 'capability/broker sandbox design', 'narrow handles/brokered operations reduce ambient authority if the worker cannot escape to broader APIs', 'attempted unauthorized resource access', 'pass explicit descriptors/capabilities and keep broker policy minimal', 'capabilities are only least-privilege if ambient escape paths are removed', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('seccomp_broker', 'sandboxing', 'seccomp plus pathname broker', 'seccomp can restrict syscalls but complex filesystem policy is better enforced by a trusted resolver/broker', 'forbidden-syscall tests plus path policy tests', 'keep worker syscall surface small and delegate constrained opens', 'syscall filtering alone cannot express rich pathname semantics', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('secret_zero', 'secrets', 'explicit secret zeroization', 'ordinary dead stores may be optimized away and copies can outlive the intended secret lifetime', 'compiler/runtime inspection and memory lifetime review', 'use dedicated zeroization primitives and minimize copies', 'zeroization cannot erase secrets already copied to logs/dumps/other buffers', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('tls_min', 'tls', 'minimum TLS version rollout', 'hardening protocol versions can break unidentified legacy clients if changed without inventory', 'client protocol telemetry and staged handshake failures', 'inventory, canary, raise minimum, monitor and retain rollback', 'disabling certificate verification is not a compatibility fix', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('cert_clock', 'tls', 'certificate validity and system time', 'TLS validity windows are evaluated against local clock and snapshots/drift can create false not-yet-valid/expired failures', 'UTC/time-sync state versus certificate notBefore/notAfter', 'fix time synchronization before rotating valid certificates', 'trust-chain errors and validity-window errors are different', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('rate_limit_auth', 'authentication', 'authentication rate limiting', 'online guessing cost depends on per-account/source throttling and lockout design', 'attempt-rate telemetry and distributed-source tests', 'use bounded adaptive rate limits without creating trivial account-lockout DoS', 'one global IP limit is weak against distributed attacks and shared NATs', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
]

register('secure-engineering', 'security', SCENARIOS, offset=5)
