from ._scenario import Scenario, register

SCENARIOS = [
    Scenario('unit_order', 'systemd', 'systemd ordering versus requirement dependencies', 'After/Before only order jobs that are both present; Wants/Requires control pulling/failure relation', 'effective unit graph and delayed dependency tests', 'encode both ordering and requirement semantics explicitly', 'After= does not cause another unit to start', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('socket_activation', 'systemd', 'socket activation lifecycle', 'systemd can own/listen before the service starts and pass an already-open socket', 'LISTEN_FDS behavior and startup connection tests', 'make the service consume inherited descriptors correctly', 'binding the same address again conflicts with the activated socket', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('watchdog', 'systemd', 'health-backed systemd watchdogs', 'a watchdog keepalive must represent useful progress, not just a timer thread', 'journal watchdog events plus induced deadlock', 'send notifications only after checking real health', 'a blind heartbeat can survive the failure it is meant to detect', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('start_limit', 'systemd', 'restart-loop rate limiting', 'automatic restart needs bounded retry rate to avoid log/CPU storms', 'restart counts and StartLimit hits', 'combine RestartSec with start-rate limits and alerts', 'restart policy does not fix the underlying crash', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('environment', 'systemd', 'service environment isolation', 'systemd services do not inherit interactive shell startup state automatically', 'effective environment, PATH, cwd, uid/gid', 'declare paths/environment/working directory explicitly', 'sourcing a user shell profile blindly creates hidden dependencies', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('journald_rate', 'logging', 'journald rate limiting', 'runaway logs can be suppressed to protect storage/I/O', 'suppression counters and message rates', 'tune burst/interval and reduce repetition at the application', 'disabling limits entirely can make logging an outage cause', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('logrotate_fd', 'logging', 'log rotation with open file descriptors', 'renaming a log does not force a process holding the old fd to reopen the new pathname', 'inode/fd inspection before and after rotation', 'signal/reopen or use copytruncate only when its tradeoffs are acceptable', 'writing to a pathname and writing to an open inode are different', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('tmpfiles', 'filesystem-management', 'systemd-tmpfiles lifecycle rules', 'automatic creation/cleanup follows declarative age/ownership rules that can delete data if policy is wrong', 'tmpfiles config dry runs and file timestamps', 'encode only disposable paths and test cleanup policy', 'temporary-path automation is dangerous for state that is actually durable', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('nofile', 'resource-limits', 'service file-descriptor headroom', 'listeners, clients, upstreams, logs and transient fds all consume the unit limit', 'actual fd counts by type under load', 'size LimitNOFILE from modeled peak plus reserve', 'one connection is not necessarily one descriptor', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('core_dump', 'diagnostics', 'core-dump policy and limits', 'core generation depends on RLIMIT, handler policy, permissions and sandboxing beyond the binary itself', 'effective unit limits and coredump journal', 'configure limits/handler/storage deliberately', 'application intent alone cannot override a zero core limit', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('inotify_overflow', 'filesystem-events', 'inotify queue overflow', 'bursty filesystem activity can exceed the event queue and lose detailed events', 'IN_Q_OVERFLOW plus rescan comparison', 'treat overflow as a signal to resynchronize state by scanning', 'continuing from the remaining events can leave a silently inconsistent view', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('epoll_oneshot', 'eventing', 'EPOLLONESHOT rearming', 'oneshot disables further notifications until the fd is explicitly rearmed', 'event traces showing one delivery then silence', 'drain nonblocking work and MOD-rearm under clear ownership', 'readiness is not automatically reenabled', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('cgroup_cpu', 'cgroups', 'CPU throttling in cgroup v2', 'cpu.max can throttle runnable work independent of host-wide idle CPU', 'cpu.stat throttled time and latency', 'size quota or placement for required CPU budget', 'host CPU percentage can hide cgroup-local throttling', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('cgroup_memory', 'cgroups', 'memory.max local OOM', 'a cgroup can hit its hard memory cap while the host still has free memory', 'memory.events and host/cgroup memory side by side', 'right-size the cgroup and fix working-set growth', 'host free memory does not override memory.max', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('cgroup_io', 'cgroups', 'I/O controller contention', 'io.max/io.weight policy can bound or prioritize a workload at the block layer', 'per-cgroup I/O latency/throughput and controller settings', 'set policy from SLOs and validate under mixed load', 'device idle periods do not prove every cgroup has unrestricted service', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('psi', 'observability', 'Pressure Stall Information', 'PSI measures time tasks are stalled on CPU/memory/I/O pressure rather than only resource utilization', 'PSI correlated with request latency', 'use PSI as a capacity/diagnostic signal with resource-specific metrics', 'PSI identifies pressure, not the exact root cause', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('network_ns', 'namespaces', 'network namespace isolation', 'interfaces/routes/firewall state are namespace-local and can differ from the host view', '`ip`/route/socket inspection inside the namespace', 'debug from the namespace where the process runs', "host networking state is not necessarily the container's networking state", ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('mount_ns', 'namespaces', 'mount namespace isolation', 'a process can see a different mount tree even with identical path strings', '/proc/*/mountinfo inside target namespace', 'enter/inspect the target namespace before changing mounts', 'host pathname existence does not prove the process sees the same filesystem', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('mount_propagation', 'namespaces', 'mount propagation modes', 'shared/slave/private mount relationships determine whether mount events cross namespace boundaries', 'mountinfo propagation flags and controlled mount tests', 'choose propagation deliberately for container/runtime needs', 'namespace creation alone does not define mount-event propagation', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('overlay_whiteout', 'overlayfs', 'overlay filesystem whiteouts', 'deletions in an upper layer can hide lower-layer files using whiteout semantics', 'layer inspection and merged-view comparison', 'reason about lower/upper layers separately during recovery/backup', 'a file missing from the merged view may still exist in a lower layer', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('tmpfs', 'memory', 'tmpfs memory accounting', 'tmpfs consumes memory/swap rather than ordinary persistent disk blocks', 'tmpfs usage plus cgroup/host memory pressure', 'bound tmpfs size and clean up lifecycle', 'fast temporary storage is not free capacity', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('inode_exhaust', 'filesystems', 'inode/metadata exhaustion', 'millions of tiny files can exhaust metadata objects while free data blocks remain', 'inode usage and filesystem metadata counters', 'remove/restructure tiny files or choose suitable filesystem layout', 'df free bytes alone are insufficient', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('ext4_commit', 'filesystems', 'journaling commit interval tradeoffs', 'longer metadata commit intervals can improve throughput but increase loss window for un-fsynced changes', 'fsync latency, journal commits, crash tests', 'set policy from durability requirements and application fsync behavior', 'mount options cannot substitute for application durability semantics', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('xfs_repair', 'filesystems', 'filesystem repair/offline requirements', 'repair tools may require unmounted/offline state and can discard unrecoverable metadata depending on options', 'filesystem health reports and vendor tooling output', 'follow filesystem-specific recovery procedure and preserve images/backups first', 'running generic repair commands blindly can worsen damage', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('mdraid_rebuild', 'storage', 'RAID rebuild interference', 'rebuild I/O competes with foreground workload and remaining devices operate under degraded redundancy', 'rebuild rate, device latency, foreground SLO', 'throttle/schedule rebuild while balancing exposure time', 'slowing rebuild forever leaves the array vulnerable longer', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('dmcrypt_cpu', 'storage', 'encryption throughput bottlenecks', 'block encryption can consume CPU or accelerator resources before the device reaches raw throughput', 'CPU crypto utilization versus device bandwidth', 'use appropriate acceleration/CPU placement and realistic benchmarks', 'raw disk benchmarks do not include encryption overhead', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('nvme_queue', 'storage', 'NVMe queue-depth saturation', 'latency rises as outstanding I/O exceeds the useful queueing point even if throughput changes little', 'latency/IOPS versus queue-depth sweep', 'choose concurrency at the throughput/latency knee', 'maximum queue depth is not an optimal operating point', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('irqbalance', 'interrupts', 'automatic IRQ balancing tradeoffs', 'moving interrupts can improve load balance but hurt cache/NUMA locality for high-rate queues', 'per-CPU IRQ counts and network/storage queue locality', 'pin or guide hot IRQs when measured locality benefits exceed balancing needs', 'irqbalance defaults are not optimal for every topology', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('nft_conntrack', 'firewall', 'conntrack table pressure', 'stateful firewall/NAT entries consume a finite table and can drop new flows when exhausted', 'conntrack count/max/drop counters', 'reduce churn, size limits, and expiration policy from workload', 'raising the table without memory planning can create a different resource problem', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('rp_filter', 'networking', 'reverse-path filtering', 'strict source validation can drop asymmetric-routed traffic that is otherwise legitimate', 'kernel counters and route lookup for packet source', 'choose strict/loose/off policy appropriate to routing design', 'packet capture on ingress does not prove the kernel will accept the packet', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('tcp_backlog', 'networking', 'listen/SYN queue pressure', 'bursty connection arrival can overflow admission queues before established-flow processing is saturated', 'listen queue drops, SYN stats, accept rate', 'improve accept responsiveness and size backlog with memory/headroom', 'increasing backlog does not fix a stalled accept loop', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('sysctl_scope', 'configuration', 'namespaced versus host sysctl scope', 'some kernel tunables are namespace-specific while others are global or constrained', 'read effective values from the target namespace and kernel docs', 'apply changes at the correct scope and record rollback', 'writing a host sysctl does not guarantee a container sees or can change it', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('capabilities', 'security', 'Linux capability bounding', 'splitting root privileges into capabilities can reduce authority but inherited/permitted/effective/bounding sets interact', '/proc capability sets and controlled denied operations', 'grant only required capabilities and drop bounding/ambient authority when possible', 'having uid 0 or one effective cap does not describe the whole privilege state', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('seccomp', 'security', 'seccomp syscall filtering', 'seccomp filters syscalls/arguments but cannot easily encode complex pathname policy after resolution', 'seccomp audit logs and syscall traces', 'combine syscall reduction with broker/namespace/filesystem policy', 'seccomp is not a general filesystem authorization engine', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('ebpf_verifier', 'observability', 'eBPF verifier rejection', 'programs must satisfy bounded-control-flow, pointer-safety and kernel-specific verifier rules before load', 'verifier log and instruction state', 'simplify proof obligations or use supported helper/data patterns', 'runtime correctness is irrelevant if the verifier cannot prove safety', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('bpf_map_pressure', 'observability', 'eBPF map capacity', 'fixed-size maps can hit entry/memory limits under high-cardinality telemetry', 'map occupancy/update failures', 'bound cardinality, evict/aggregate, or size maps deliberately', 'adding instrumentation can itself consume significant kernel memory', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
]

register('linux-infrastructure', 'linux', SCENARIOS, offset=12)
