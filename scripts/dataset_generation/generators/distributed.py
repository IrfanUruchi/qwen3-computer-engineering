from ._scenario import Scenario, register

SCENARIOS = [
    Scenario('quorum_intersection', 'quorums', 'read/write quorum intersection', 'simple quorum overlap requires R+W>N for all N replicas', 'quorum arithmetic plus adversarial disjoint-set examples', 'choose R/W to satisfy required overlap and define version semantics', 'quorum math alone is not a full consistency proof', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('fencing', 'coordination', 'fencing stale owners', 'old primaries may still reach the protected resource after a new owner is elected', 'monotonic token logs at the resource', 'enforce increasing fencing tokens at the side-effect boundary', 'lease/election state alone cannot stop a partitioned stale process', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('lease_clock', 'leases', 'lease safety under clock uncertainty', 'independent wall-clock comparisons can create overlapping ownership belief', 'clock offset/uncertainty and lease timelines', 'use a protocol with explicit clock bounds or avoid wall-clock mutual exclusion', 'NTP synchronization is not itself a safety proof', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('tombstone_gc', 'replication', 'tombstone garbage collection', 'deletion metadata removed before a stale replica returns can allow old data to resurrect', 'replica repair history and tombstone age', 'retain deletion evidence through the safe repair window or rebuild stale replicas', 'storage reclamation can violate delete semantics', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('read_repair', 'replication', 'opportunistic read repair', 'reads can repair replicas they touch but cold keys may never be read', 'repair counts and cold-key divergence', 'combine read repair with background anti-entropy', 'read repair alone does not guarantee complete convergence', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('hinted_handoff', 'replication', 'hinted handoff backlog', 'temporary writes queued for unavailable replicas can grow and later create recovery bursts', 'hint queue age/size and replay throughput', 'bound hints and throttle replay with normal anti-entropy fallback', 'hints are not durable forever unless explicitly designed that way', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('merkle', 'replication', 'Merkle-tree anti-entropy', 'hierarchical hashes localize divergent key ranges without full dataset comparison', 'root/subtree digest comparisons and reconciliation counts', 'use deterministic partitioning/encoding and repair only mismatched ranges', 'Merkle trees do not define conflict-resolution semantics', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('vector_clock', 'versioning', 'vector-clock causality', 'concurrent versions cannot always be totally ordered by wall-clock timestamp', 'version-vector comparison on divergent writes', 'preserve concurrent versions or apply explicit merge policy', 'last timestamp wins can discard causally concurrent updates', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('lamport', 'versioning', 'Lamport clock ordering', 'logical clocks order events consistently with causality but do not measure real elapsed time', 'event histories and clock increments', 'use them for ordering/tie-breaking within their model', 'a larger Lamport value does not mean more real time passed', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('gossip', 'membership', 'gossip dissemination', 'membership/update information spreads probabilistically and is not instant', 'rounds-to-convergence and fanout measurements', 'size fanout/interval from convergence and traffic goals', 'one gossip round does not guarantee every node learned the update', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('phi_detector', 'membership', 'adaptive failure suspicion', 'timeout-only detectors can misclassify nodes under variable latency, while accrual detectors express suspicion continuously', 'heartbeat interarrival distribution and suspicion scores', 'tune from observed delay distributions and combine with quorum/consensus rules', 'failure suspicion is not proof of node death', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('split_brain', 'consensus', 'split-brain prevention', 'two partitions must not both obtain authority for the same exclusive state', 'partition tests and ownership tokens', 'require quorum/consensus plus fencing at side-effect boundaries', 'reconnecting later cannot undo conflicting external writes', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('election_timeout', 'consensus', 'election timeout stability', 'timeouts too close to normal heartbeat delays cause unnecessary elections', 'heartbeat/RTT/pause distributions and election rate', 'randomize timeouts with margin above routine delays', 'shorter detection is not always better if it destabilizes leadership', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('snapshot_install', 'consensus', 'snapshot installation', 'a lagging follower may need a complete state snapshot after required log entries were compacted', 'snapshot index/term and interrupted-transfer tests', 'install only complete validated snapshots atomically then resume log replication', 'partial or stale snapshot install can roll state backward/corrupt state', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('log_compaction', 'consensus', 'log compaction boundaries', 'state can be compacted only after durable snapshot/application state represents the committed prefix', 'commit index, snapshot index, restore tests', 'compact behind a verified snapshot boundary', "deleting log entries because they are 'old' can destroy recovery", ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('membership_change', 'consensus', 'configuration change safety', 'changing voters can temporarily create disjoint majorities unless the protocol uses a safe transition', 'partition tests during reconfiguration', "use the consensus protocol's joint/stepwise membership procedure", 'editing the member list on every node is not a consensus algorithm', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('saga', 'transactions', 'saga compensation', 'distributed steps cannot be rolled back atomically across independent services', 'durable workflow state and compensation retries', 'make forward/compensating actions idempotent and observable', 'compensation is not equivalent to ACID rollback', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('outbox', 'transactions', 'transactional outbox', 'database commit and broker publish cannot be made atomic by ordering two independent operations', 'crash tests around commit/publish boundary', 'store outbox intent in the local transaction and relay at-least-once', 'publish success before relay ack can create duplicates', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('inbox_dedupe', 'transactions', 'consumer deduplication', 'at-least-once delivery can repeat messages after crashes/retries', 'message IDs and duplicate-delivery fault tests', 'persist processed IDs/result atomically with effects when needed', 'acknowledgment timing alone cannot guarantee exactly-once effects', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('idempotency', 'transactions', 'idempotent request keys', 'retries need a stable logical-operation identity across client uncertainty', 'crash-after-side-effect tests and key/result storage', 'bind one key to one request payload/result durably', 'allowing the same key with different parameters breaks semantics', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('consistent_hash', 'partitioning', 'consistent-hash ring balance', 'few token positions create uneven key ranges even if movement on membership change is limited', 'keys/bytes/QPS per node', 'use enough virtual positions or another balanced scheme', 'consistent hashing does not automatically mean balanced hashing', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('rendezvous', 'partitioning', 'rendezvous hashing', 'key/node pair scores choose deterministic top replicas with limited remapping on membership change', 'cross-client selection consistency and remap ratio', 'use stable encoding/hash and shared membership view', 'different node IDs/encodings across clients destroy determinism', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('hot_partition', 'partitioning', 'hot-key partition overload', 'uniform key counts do not prevent one popular key from saturating a shard', 'per-key/QPS distribution and shard utilization', 'split/replicate/cache hot keys or change partitioning semantics', "adding cold keys does not dilute one hot key's load", ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('backpressure', 'streaming', 'consumer lag backpressure', 'producer rate above sustainable consumer rate causes lag/queue growth until bounded resources fill', 'arrival/consume rate and lag age', 'throttle/admit or scale consumers and bound retention', 'larger queues do not create processing capacity', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('watermark', 'streaming', 'event-time watermark tradeoffs', 'late-event tolerance determines when windows are considered complete and how long state must be retained', 'lateness distribution and correction rate', 'set watermark policy from business lateness and state budget', 'processing-time completion can silently drop valid late events', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('exactly_once', 'streaming', 'exactly-once claims', 'end-to-end exactly-once effects require coordinated state/offset/effect semantics, not just broker delivery mode', 'crash tests around state update and offset commit', 'make state transitions transactional/idempotent across the real side-effect boundary', 'a broker setting alone cannot guarantee external exactly-once effects', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('clock_order', 'time', 'wall-clock event ordering', 'clock skew can make later events carry earlier timestamps across nodes', 'clock offsets and causal message relationships', 'use logical/causal ordering when correctness requires it', 'timestamps from unsynchronized clocks are not a total causal order', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('replica_lag', 'replication', 'replica apply lag', 'a replica can receive data fast enough but apply it slowly due to storage/CPU contention', 'receive queue versus apply queue and local resource metrics', 'isolate/accelerate apply path or throttle competing work', 'low network utilization does not rule out replication lag', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('read_your_writes', 'consistency', 'read-your-writes session guarantees', 'a client routed to a stale replica can fail to observe its own acknowledged write', 'session routing/version tests', 'pin sessions, forward reads, or carry version constraints', 'eventual consistency does not automatically provide session guarantees', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('retry_storm', 'resilience', 'coordinated retry storms', 'many clients failing together and using deterministic retry schedules re-synchronize load', 'retry timestamp histograms and dependency saturation', 'use exponential backoff with jitter and server retry guidance', 'exponential backoff without randomness can still herd', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
]

register('distributed-systems', 'distributed', SCENARIOS, offset=21)
