from ._scenario import Scenario, register

SCENARIOS = [
    Scenario('pmtu_blackhole', 'ip', 'PMTU black holes', 'oversized packets cannot pass a smaller path MTU and missing ICMP prevents sender adjustment', 'packet-size probes and missing ICMP PTB/frag-needed messages', 'fix feedback/MTU or apply accurate edge MSS policy', 'small packets succeeding does not prove larger packets fit', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('tcp_zerowin', 'tcp', 'TCP zero-window stalls', 'the receiver advertises no buffer space and intentionally stops the sender', 'packet capture windows plus receiver drain behavior', 'fix downstream consumer/buffer pressure', 'zero-window flow control is different from congestion loss', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('nagle_ack', 'tcp', 'Nagle and delayed-ACK interaction', 'small unacknowledged writes can wait while the peer delays ACKs', 'packet/write timing and controlled TCP_NODELAY or batching A/B', 'batch at the application or disable Nagle only where latency needs justify it', 'TCP_NODELAY is not a universal throughput optimization', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('bdp_window', 'tcp', 'bandwidth-delay-product window limits', 'the sender needs enough in-flight bytes to fill a high-bandwidth/high-RTT path', 'BDP calculation, advertised window, scaling and buffer limits', 'size/tune window buffers appropriately', 'low packet loss does not prove the flow is not window-limited', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('syn_backlog', 'tcp', 'SYN/listen backlog pressure', 'connection bursts can overflow half-open or accept queues before established traffic is saturated', 'SYN/listen drops and accept rate', 'improve accept responsiveness, backlog sizing, and front-door protection', 'healthy established flows do not rule out admission failure', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('syn_cookies', 'tcp', 'SYN cookie tradeoffs', 'cookies reduce half-open state pressure but do not provide general application capacity', 'backlog pressure plus cookie activation counters', 'use them as one layer with normal capacity/backlog controls', 'SYN cookies do not stop established-connection exhaustion', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('time_wait', 'tcp', 'TIME_WAIT accumulation', 'active close leaves connection tuples reserved to protect delayed segments and sequence reuse', 'socket-state counts and connection churn', 'reuse/pool connections and size ephemeral space/NAT capacity', 'blindly shortening TIME_WAIT can break correctness assumptions', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('ephemeral_ports', 'tcp', 'ephemeral-port exhaustion', 'high outbound connection churn can consume usable local/NAT source-port tuples', 'local port range, TIME_WAIT/active states, allocation errors', 'reuse connections and increase safe tuple capacity', 'CPU/bandwidth headroom does not create new 4-tuples', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('keepalive', 'tcp', 'TCP keepalive semantics', 'keepalive detects transport peer reachability after idle periods, not application progress', 'probe timing and peer/network behavior', 'configure idle/interval/count from detection requirements and use app heartbeat for semantic health', 'transport liveness is not application health', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('retrans_rto', 'tcp', 'RTO-driven latency', 'loss can trigger retransmission timeout when faster loss recovery lacks enough signals', 'packet capture, RTT/RTO, retransmission timing', 'reduce loss and tune protocol/stack only with evidence', 'one lost packet does not always recover on the same timescale', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('ecn', 'congestion', 'ECN negotiation and marking', 'endpoints can react to congestion marks without relying only on packet drops', 'handshake negotiation, CE/ECE/CWR and queue metrics', 'enable end-to-end where supported and validate congestion-control behavior', 'ECN changes signalling, not available capacity', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('bufferbloat', 'queueing', 'bufferbloat', 'oversized queues keep links busy while adding large standing latency under load', 'latency versus queue occupancy during saturation', 'use active queue management or right-size buffers', 'zero packet loss can coexist with terrible latency', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('rss', 'host-networking', 'RSS flow distribution', 'NIC hash/queue mapping determines which CPUs receive flows and can become imbalanced with elephant flows', 'per-queue packet counts and CPU/IRQ load', 'tune RSS/affinity and account for flow distribution', 'equal queue count does not guarantee equal load', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('rps_rfs', 'host-networking', 'software receive steering', 'RPS/RFS can move packet processing toward CPUs but add cross-CPU queueing/cache costs', 'softirq CPU distribution and application locality', 'use only when measured placement improves throughput/latency', 'software steering can worsen locality if layered blindly over hardware RSS', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('udp_trunc', 'udp', 'UDP datagram truncation', 'a receive buffer smaller than the datagram can return only a prefix while the remainder is discarded', 'MSG_TRUNC and datagram-size tests', 'detect truncation and reject/resize according to protocol maximum', 'UDP is message-oriented but the application buffer can still be too small', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('udp_loss', 'udp', 'UDP loss under receive-queue pressure', 'datagrams can be dropped when socket/kernel queues overflow because there is no transport retransmission', 'socket drop counters and receive-queue occupancy', 'increase processing efficiency/buffer within bounds or add application reliability', 'a sender success return does not mean the receiver accepted the datagram', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('dns_ttl', 'dns', 'DNS caching and TTL staleness', 'authoritative changes propagate only as caches expire and runtimes may add their own caching policy', 'queries at authoritative, recursive, OS and runtime layers', 'fix the layer violating desired refresh behavior', 'low authoritative TTL does not force every client runtime to refresh promptly', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('dns_negative', 'dns', 'negative DNS caching', 'NXDOMAIN/no-data answers can be cached and keep failures visible after a record is created', 'negative-cache TTL and resolver-layer queries', 'wait/flush/fix TTL policy according to DNS semantics', 'creating the record does not instantly invalidate prior negative caches', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('http2_hol', 'http', 'HTTP/2 over TCP head-of-line blocking', "application streams are multiplexed but all bytes still depend on TCP's ordered delivery", 'packet loss and per-stream stall timing', 'reduce loss or use transports with independent stream delivery where appropriate', 'multiplexing above TCP does not remove TCP-level HOL', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('quic_migration', 'quic', 'QUIC connection migration', 'connection identity is not bound only to a 4-tuple, allowing path changes with validation', 'connection-ID and PATH_CHALLENGE/response traces', 'validate the new path before fully trusting it', 'accepting any new source address without validation enables spoofing', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('nat_rebind', 'nat', 'NAT rebinding', 'external source tuples can change when NAT mappings expire or network changes', 'packet tuples and application session identity', 'bind sessions to protocol identities rather than assuming stable source ports', 'source IP/port is not always a durable client identity', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('tls_sni', 'tls', 'TLS SNI virtual hosting', 'servers choose certificates/config based on the requested server name before HTTP Host is available', 'ClientHello SNI and presented certificate', 'send/route the intended SNI and configure certificate names', 'HTTP Host cannot repair a certificate chosen earlier in TLS', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('tls_alpn', 'tls', 'ALPN protocol negotiation', 'client and server must agree on an application protocol during TLS negotiation', 'ClientHello/ServerHello ALPN values', 'configure overlapping protocol sets and correct fallback', 'a successful TLS handshake does not imply the expected app protocol was selected', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('mtls_san', 'tls', 'mTLS endpoint identity', 'a trusted certificate chain still must contain the identity being verified', 'SAN entries versus requested DNS/service identity', 'issue/use certificates with correct identities', 'trusted issuer does not make a certificate valid for every hostname', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('icmp_rate', 'ip', 'ICMP rate limiting', 'control-plane/error messages can be intentionally rate-limited under bursts', 'ICMP counters and controlled probe rates', 'interpret missing ICMP with rate-limit policy in mind', 'absence of every error response does not prove the path did not see the offending packet', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('fragmentation', 'ip', 'IP fragmentation costs', 'fragmentation creates multiple pieces and loss of one can invalidate the whole original datagram', 'fragment counters and packet captures', 'avoid oversized datagrams and rely on PMTU where possible', 'fragmentation is not a free substitute for proper MTU handling', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('bbr_cubic', 'congestion', 'congestion-control algorithm tradeoffs', 'different algorithms estimate/use bandwidth, RTT and loss differently, affecting queues and fairness', 'controlled same-path throughput/latency/fairness tests', 'choose algorithm based on environment and policy', 'one benchmark path cannot establish universal superiority', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('reverse_path', 'routing', 'asymmetric routing with strict reverse-path checks', 'strict source validation can drop packets whose best reverse route differs from ingress', 'route lookup and rp_filter counters/settings', 'choose validation mode consistent with routing/security design', 'seeing a packet arrive at the NIC does not mean the IP stack accepts it', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('anycast', 'routing', 'anycast path changes', 'the same service IP can route a client to different sites as routing changes', 'traceroute/site identifiers and session timing', 'design sessions for site changes or pin at higher layers when required', 'one IP address does not guarantee one physical backend', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('mtu_tunnel', 'tunnels', 'encapsulation MTU overhead', 'tunnel headers reduce effective payload MTU below the underlay interface MTU', 'inner/outer captures and interface/tunnel MTUs', 'set consistent inner MTU or working PMTU/MSS strategy', 'matching underlay interface MTUs is insufficient after encapsulation', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
]

register('networking', 'networking', SCENARIOS, offset=18)
