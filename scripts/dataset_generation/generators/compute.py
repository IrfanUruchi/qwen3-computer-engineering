from ._scenario import Scenario, register

SCENARIOS = [
    Scenario('grad_accum', 'training', 'gradient accumulation scheduling', 'effective batch accumulation requires multiple backward passes before one optimizer update', 'optimizer-step count and gradient scale', 'step once per accumulation window and align clipping/scheduler updates', 'stepping every microbatch defeats accumulation', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('activation_ckpt', 'training-memory', 'activation checkpointing', 'recomputation trades extra forward compute for lower saved-activation memory', 'memory breakdown and step-time A/B', 'checkpoint memory-heavy blocks when activations dominate', 'checkpointing does little if parameters/optimizer state dominate', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('optimizer_shard', 'distributed-training', 'optimizer-state sharding', 'replicated optimizer states can dominate per-rank memory and be partitioned across ranks', 'peak memory by category and communication timeline', 'shard optimizer state when its memory term is limiting', 'memory savings can cost communication/checkpoint complexity', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('grad_bucket', 'distributed-training', 'gradient bucket sizing', 'smaller buckets can overlap communication earlier while too-small buckets increase collective overhead', 'backward/collective timeline across bucket sizes', 'tune at the step-time knee for the topology/model', 'one bucket size is not universally optimal', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('allreduce_topology', 'distributed-training', 'all-reduce topology limits', 'collective throughput follows actual link topology and slow paths rather than advertised GPU compute', 'P2P/collective microbenchmarks plus topology map', 'place/partition work to reduce slow-link traffic and overlap communication', 'slot labels do not equal achieved peer bandwidth', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('pipeline_bubble', 'distributed-training', 'pipeline parallel bubbles', 'stage imbalance and too few microbatches leave devices idle between pipeline waves', 'per-stage timeline and utilization', 'balance stage work and choose enough microbatches within memory limits', 'adding stages can reduce efficiency if bubbles dominate', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('tensor_parallel', 'distributed-training', 'tensor-parallel communication', 'splitting one layer across devices adds frequent activation/partial-result collectives', 'collective bytes and kernel/communication overlap', 'use only when memory/compute benefits justify interconnect cost', 'more devices can slow a layer on weak interconnect', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('data_parallel', 'distributed-training', 'data-parallel scaling efficiency', 'each rank replicates model compute but gradients require synchronization every step', 'single-rank versus multi-rank step time and all-reduce fraction', 'scale while overlap/interconnect keeps synchronization fraction acceptable', 'linear device count does not imply linear throughput', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('straggler', 'distributed-training', 'slow-rank synchronization', 'synchronous steps wait for the slowest participant', 'per-rank phase timelines', 'fix the rank-specific input/thermal/network/compute bottleneck', 'average GPU utilization can hide one-rank stalls', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('mixed_precision', 'numerics', 'mixed-precision overflow/underflow', 'reduced exponent/mantissa range can create non-finite or vanished gradients', 'gradient finiteness and high-precision reference comparison', 'use appropriate precision/loss scaling and monitor numerics', 'faster low precision is not correct if updates become non-finite', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('loss_scaler', 'numerics', 'dynamic loss scaling', 'the scaler must skip optimizer updates on overflow and adjust scale over time', 'forced-overflow tests and optimizer-state changes', 'unscale/check, skip bad steps, reduce scale, then regrow after stable steps', 'advancing optimizer state on overflow silently corrupts training', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('kernel_launch', 'gpu-performance', 'GPU kernel launch overhead', 'thousands of tiny kernels can spend more time launching/synchronizing than computing', 'GPU timeline of gaps and kernel durations', 'fuse/batch compatible work while preserving dependencies', 'creating more streams does not remove launch overhead', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('kernel_fusion', 'gpu-performance', 'kernel fusion tradeoffs', 'fusion can cut global-memory traffic/launches but raise register pressure and lower occupancy', 'memory bytes, kernel count, registers, occupancy, latency', 'fuse only where end-to-end timing improves', 'fewer kernels are not automatically faster', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('pinned_memory', 'gpu-transfer', 'pinned host-memory transfers', 'asynchronous DMA generally benefits from page-locked host buffers and can otherwise require staging', 'pageable versus pinned bandwidth and overlap timeline', 'use bounded pinned pools on hot transfer paths', 'pinning too much memory harms host memory management', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('stream_sync', 'gpu-scheduling', 'unintended CUDA stream synchronization', 'default-stream or device-wide synchronization can serialize otherwise independent work', 'per-stream profiler timeline and sync API traces', 'use explicit events/dependencies and intended streams', 'multiple streams do not guarantee concurrency', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('memory_frag', 'gpu-memory', 'GPU allocator fragmentation', 'enough total free bytes may exist while no suitable contiguous block is available to satisfy a request', 'allocator reserved/active/free block stats', 'reuse pools, reduce size churn, or tune allocator strategy', 'total free memory alone does not prove a large allocation can succeed', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('dataloader', 'input-pipeline', 'GPU input starvation', 'the accelerator idles when host decode/storage/augmentation cannot keep the ready queue filled', 'end-to-end loader/transfer/kernel timeline', 'parallelize/cache/preprocess and use bounded prefetch', 'raising GPU clocks cannot fix an empty input queue', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('prefetch_batches', 'input-pipeline', 'batch prefetch depth', 'too little prefetch exposes host jitter while too much consumes memory and increases stale queued work', 'queue occupancy, memory, and step-time variance', 'choose bounded prefetch at the latency/memory knee', 'deeper queues do not increase source throughput', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('checkpoint_atomic', 'checkpointing', 'atomic training checkpoints', 'a partially written checkpoint must never be selected as the newest resumable state', 'interrupted-write tests and manifest/hash validation', 'write versioned temp state then atomically publish a complete manifest', 'newest filename is not necessarily newest complete checkpoint', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('checkpoint_shard', 'checkpointing', 'sharded checkpoint completeness', 'distributed checkpoints require all required rank shards and consistent metadata', 'missing-shard fault tests and manifest validation', 'commit a global completeness manifest after all shards are durable', 'one successful rank write does not make the checkpoint recoverable', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('kv_cache', 'inference', 'KV-cache memory growth', 'autoregressive context length and batch size grow retained attention state', 'memory versus tokens/batch and cache hit/reuse metrics', 'budget context/batch, page/evict cache where supported', 'model weights are not the only inference memory term', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('continuous_batch', 'inference', 'continuous batching scheduling', 'mixing requests of different lengths can improve utilization but complicate latency/fairness and KV-cache packing', 'tokens/s plus per-request latency and queue age', 'schedule with bounded waiting/fairness and memory admission', 'max throughput batching can starve short interactive requests', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('quant_error', 'quantization', 'quantization error validation', 'lower-bit weights/activations trade memory/throughput for approximation error', 'task metrics and layer-wise error against higher-precision reference', 'calibrate/evaluate on representative data and exempt sensitive layers if needed', 'smaller model files do not prove acceptable model quality', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('moe_imbalance', 'mixture-of-experts', 'MoE expert load imbalance', 'router skew can overload a few experts while others idle, creating communication and capacity bottlenecks', 'tokens per expert and dispatch/all-to-all timeline', 'use load-balancing loss/capacity/router tuning', 'aggregate FLOPs can look available while one expert path saturates', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('cpu_offload', 'training-memory', 'CPU offload tradeoffs', 'moving optimizer/parameter state to host saves VRAM but adds PCIe transfers and host-memory/CPU pressure', 'transfer bytes, PCIe utilization, step time, host memory', 'offload only when memory relief outweighs communication cost', 'free VRAM alone is not a performance win', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
    Scenario('compile_graph', 'gpu-performance', 'graph compilation/capture amortization', 'capturing/compiling execution reduces launch/framework overhead only if shapes/control flow are stable enough to reuse it', 'compile time, cache hit/recompile count, steady-state latency', 'use capture/compile on stable hot paths and account for warmup', 'one fast steady-state step can hide huge recompilation cost', ('A production service is under representative load', 'A reproducible integration test exposes the issue', 'An incident appears only after the system has been running for some time')),
]

register('compute-model-infrastructure', 'compute', SCENARIOS, offset=2)
